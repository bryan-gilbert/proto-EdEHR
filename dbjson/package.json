{
  "name": "ed-db",
  "version": "1.0.0",
  "description": "Prototype EdEHR DB",
  "author": "Bryan Gilbert",
  "license": "MIT",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "install-all": "npm install",
    "start": "json-server --watch ./db.json --port 5678"
  },
  "dependencies": {
    "json-server": "^0.14.0"
  }
}
