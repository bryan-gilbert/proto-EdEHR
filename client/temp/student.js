// import shop from '../../api/shop'

const state = {
}

const getters = {
  sample: (state, getters, rootState) => {
  },

  sample2: (state, getters) => {
  }
}

const actions = {
  actionA ({ commit, state }, products) {
  },
  actionB ({ commit, state }, products) {
  }
}

const mutations = {
}

export default {
  namespaced: false,
  state,
  getters,
  actions,
  mutations
}
